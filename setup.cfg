[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = 
    --cov=src/ai_as_me
    --cov-report=html
    --cov-report=term-missing
    -v
