# AI-as-Me v3.4 Dashboard 用户指南

**版本**: v3.4.0  
**更新日期**: 2026-01-16

---

## 快速开始

### 启动 Dashboard

```bash
# 方式 1: 直接启动
python -m ai_as_me.dashboard.app

# 方式 2: 使用 uvicorn
uvicorn ai_as_me.dashboard.app:app --reload --port 8000
```

访问: http://localhost:8000

---

## 功能模块

### 1. 首页 (/)

**功能**: 系统概览和快速导航

- 系统状态监控
- 快速统计数据
- 导航到各功能模块

### 2. 灵感池 (/inspirations.html)

**功能**: 管理灵感和探索性想法

**操作**:
- 查看所有灵感
- 按状态筛选 (active/incubating/archived)
- 按成熟度筛选
- 添加新灵感
- 更新灵感状态

**API 端点**:
```
GET  /api/inspirations              # 列表
GET  /api/inspirations/{id}         # 详情
POST /api/inspirations              # 创建
PUT  /api/inspirations/{id}         # 更新
```

### 3. 规则管理 (/rules.html)

**功能**: 查看和管理 Soul 规则

**规则类型**:
- **Core Rules**: 人类定义的核心规则
- **Learned Rules**: AI 自创的规则

**操作**:
- 查看规则列表
- 查看规则详情
- 查看规则版本历史
- 回滚规则版本

**API 端点**:
```
GET  /api/rules                     # 列表
GET  /api/rules/{id}                # 详情
GET  /api/rules/{id}/versions       # 版本历史
POST /api/rules/{id}/rollback       # 回滚
```

### 4. 统计图表 (/stats.html)

**功能**: 可视化进化统计数据

**指标**:
- 规则应用频率
- 规则有效性评分
- 模式识别准确率
- 进化趋势图

**API 端点**:
```
GET  /api/stats?days=7              # 统计数据
```

### 5. 日志查看器 (/logs.html)

**功能**: 查询和导出系统日志

**操作**:
- 实时日志流
- 按级别筛选 (INFO/WARNING/ERROR)
- 按模块筛选
- 时间范围查询
- 导出日志 (JSON/CSV)

**API 端点**:
```
GET  /api/logs?limit=100            # 查询
GET  /api/logs/export?format=json   # 导出
GET  /api/logs/stream               # SSE 流
```

---

## API 文档

### 健康检查

```bash
curl http://localhost:8000/health
```

响应:
```json
{
  "status": "ok",
  "version": "3.4.0"
}
```

### 灵感池 API

#### 列表灵感

```bash
curl "http://localhost:8000/api/inspirations?status=active&min_maturity=0.5"
```

#### 添加灵感

```bash
curl -X POST http://localhost:8000/api/inspirations \
  -H "Content-Type: application/json" \
  -d '{
    "content": "探索新的进化策略",
    "priority": "high",
    "tags": ["evolution", "strategy"]
  }'
```

### 规则 API

#### 列表规则

```bash
curl http://localhost:8000/api/rules
```

响应:
```json
{
  "core": [
    {
      "id": "base",
      "category": "core",
      "content": "...",
      "created": "2026-01-15T10:00:00"
    }
  ],
  "learned": [
    {
      "id": "pattern-001",
      "category": "learned",
      "confidence": 0.85,
      "source": "experience-123",
      "created": "2026-01-16T02:00:00"
    }
  ]
}
```

### 统计 API

```bash
curl "http://localhost:8000/api/stats?days=7"
```

响应:
```json
{
  "rule_frequency": {
    "rule_001": 5.2,
    "rule_002": 3.1
  },
  "effectiveness": {
    "rule_001": 0.80,
    "rule_002": 0.60
  },
  "pattern_accuracy": 0.85
}
```

### 日志 API

#### 查询日志

```bash
curl "http://localhost:8000/api/logs?limit=10&level=ERROR"
```

#### 导出日志

```bash
curl "http://localhost:8000/api/logs/export?format=json&start=2026-01-15&end=2026-01-16" \
  -o logs.json
```

---

## 配置

### 日志配置

编辑 `config/logging.yaml`:

```yaml
version: 1
formatters:
  json:
    class: ai_as_me.log_system.formatter.JSONFormatter
handlers:
  file:
    class: logging.handlers.RotatingFileHandler
    filename: logs/agent.log
    maxBytes: 10485760  # 10MB
    backupCount: 5
loggers:
  ai_as_me:
    level: INFO
    handlers: [file]
```

### Dashboard 配置

环境变量:

```bash
export DASHBOARD_HOST=0.0.0.0
export DASHBOARD_PORT=8000
export DASHBOARD_RELOAD=true
```

---

## 故障排查

### Dashboard 无法启动

**问题**: `ModuleNotFoundError: No module named 'ai_as_me.dashboard'`

**解决**:
```bash
pip install -e .
```

### API 返回 404

**问题**: 所有 API 端点返回 404

**解决**: 检查路由注册
```python
# src/ai_as_me/dashboard/app.py
app.include_router(inspirations.router, prefix="/api")
```

### 日志文件过大

**问题**: `logs/agent.log` 文件过大

**解决**: 配置日志轮转
```yaml
handlers:
  file:
    maxBytes: 10485760  # 10MB
    backupCount: 5
```

---

## 性能优化

### 响应时间目标

| 端点 | 目标 | 当前 |
|------|------|------|
| /health | <10ms | 8.9ms ✅ |
| /api/inspirations | <50ms | TBD |
| /api/rules | <50ms | TBD |
| /api/stats | <100ms | TBD |
| /api/logs | <100ms | TBD |

### 优化建议

1. **启用缓存**: 使用 Redis 缓存统计数据
2. **分页查询**: 限制单次查询数量
3. **异步处理**: 使用 async/await
4. **索引优化**: 为日志查询添加索引

---

## 安全建议

1. **生产环境**: 禁用 CORS `allow_origins=["*"]`
2. **认证**: 添加 API 认证机制
3. **HTTPS**: 使用 HTTPS 协议
4. **限流**: 添加 API 限流

---

## 更新日志

### v3.4.0 (2026-01-16)
- ✅ 统一日志系统
- ✅ Web Dashboard 基础架构
- ✅ 灵感池 API
- ✅ 规则管理 API
- ✅ 统计 API
- ✅ 日志 API
- ⏳ 完整测试覆盖 (Epic 4)

---

## 反馈与支持

- **GitHub Issues**: https://github.com/jodykwong/AI-as-Me/issues
- **文档**: `_bmad-output/planning-artifacts/`
- **示例**: `tests/integration/test_dashboard_e2e.py`

---

*AI-as-Me - 让 AI 自己进化，每次迭代更好* 🚀
